apiVersion: v1
kind: Service
metadata:
  name: group-5-service
  labels:
    app: group-5-app
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: group-5-app